// function stringLength(str){
//     let length = str.length
//     console.log(`the length of "${str}" is:`, length)
//     return str.length
// }

// stringLength("willynilly")

stringLength = (str) => {
    let length = str.length
    document.write(`the length of "${str}" is:`, length)
    return str.length
}

stringLength("willynilly")